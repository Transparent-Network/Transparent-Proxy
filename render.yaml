services:
  - type: web
    name: transparent-proxy
    env: node
    plan: free
    branch: main
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
